class Price:
	def __init__(self, tenor, broker, direction, p, firm, qty, client):
    	#self.broker = broker
		self.tenor = tenor
		self.direction = direction
		self.p = p
		self.firm = firm
		self.qty = qty
		self.client = client
		self.broker = broker
    